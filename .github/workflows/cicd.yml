name: CICD
on:
  push:
    branches: [develop]

jobs:
  build:
    runs-on: [ubuntu-latest]
    steps:
      - name: Checkout Source
        uses: actions/checkout@v4
      - name : Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
      - name: Build project
        run: mvn clean install
      - name: Docker Login
        run: docker login -u ${{secrets.DOCKHUB_UID}} -p ${{secrets.DOCKHUB_PWD}}
      - name: Build docker image
        run: docker build -t itzmedinesh/music-service .
      - name: Push image to docker hub
        run: docker push itzmedinesh/music-service:latest

  deploy:
    needs: build
    runs-on: [music-run]
    steps:
      - name: Pull image from docker hub
        run: docker pull itzmedinesh/music-service:latest
      - name: Delete container if exists
        run: docker rm -f music-service-container
      - name: Run as container
        run: docker run -d -p 8080:8080 --name music-service-container itzmedinesh/music-service